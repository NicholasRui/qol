! note: initial lines of file can contain comments
!
            36 -- model for mesa/star, mlt convection velocity (mlt_vc). cgs units. lnd=ln(density), lnT=ln(temperature), lnR=ln(radius), L=luminosity, dq=fraction of xmstar=(mstar-mcenter) in cell; remaining cols are mass fractions.

<<ATTRIBUTES_TEXT>>
<<MODEL_TABLE>>
        previous model

               previous n_shells      <<PREVIOUS_N_SHELLS>>
           previous mass (grams)      <<PREVIOUS_MASS_GRAMS>>
              timestep (seconds)      <<TIMESTEP_SECONDS>>
               dt_next (seconds)      <<DT_NEXT_SECONDS>>

